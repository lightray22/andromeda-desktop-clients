#!/usr/bin/env bash
set -e

if ! [ -e build ]; then mkdir build; fi; cd build

cmake .. -DCMAKE_BUILD_TYPE=Release "$@"

cmake --build . -j$(nproc)

if [ "$1" == "inst" ]; then
   sudo cmake --install .
fi
